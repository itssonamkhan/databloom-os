import {notFound} from "next/navigation";import PowerBIPractice from "@/components/power-bi/PowerBIPractice";import {getPowerBILesson,powerBILessons} from "@/lib/powerBILessons";import {daxLessons,getDAXLesson} from "@/lib/daxFormulas";
const all=[...powerBILessons,...daxLessons];export function generateStaticParams(){return all.map(x=>({id:x.id}))}export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const lesson=getPowerBILesson(id)??getDAXLesson(id);if(!lesson)notFound();const index=all.findIndex(x=>x.id===id);return <PowerBIPractice lesson={lesson} nextLesson={all[index+1]}/>}
